# 業務要件

このドキュメントでは、VRP PoC における業務上の要件を定義する。

## 1. ドキュメントの目的

本ドキュメントの目的は、`00_problem_definition.md` で定義した VRP PoC の問題設定を、業務で利用価値のある PoC 要件へ落とし込むことである。

本ドキュメントでは、「どのような業務課題に対して」「どのような価値を返す PoC を作るのか」を明確にし、その価値提供に必要となる入力データ、出力データ、最適化の前提条件、評価観点を整理する。

特に本 PoC では、単に最適化モデルを実装すること自体を目的とするのではなく、配送計画担当者、実配送運転手、導入判断者といった利用者が、実道路ネットワークを前提とした配送計画の価値を判断できることを重視する。

そのため、本ドキュメントでは以下を整理対象とする。

- 想定する業務シナリオ
- 解決したい業務課題
- PoC が提供する業務価値
- 業務上意味を持つ入力データと出力データ
- PoC として成立させるために必要な基盤要件
- 業務上の成功基準および PoC の可否判断基準

これにより、本 PoC を「実道路データを使った最適化の技術検証」で終わらせず、業務適用可能性を判断できる社内 PoC レベルの成果物として位置付ける。

## 2. 想定業務シナリオ

本 PoC は、配送計画を前日に作成し、翌日の配送業務へ反映する業務シナリオを想定する。

本 PoC の開始条件は、翌日の配送対象データが確定しており、単一デポに紐づく稼働可能台数レンジといった当日の運用条件を参照できる状態であることとする。

想定する通常時の業務の流れは以下のとおりである。

1. 前日に、翌日の配送対象データが一括で確定する。入力データには、配送先住所と各配送先に対応する荷物個数が含まれる。
2. 配送計画担当者が、配送対象データと当日の運用条件を確認し、VRP PoC を実行する。稼働可能台数の下限値と上限値は、単一デポに紐づくマスタ情報として保持し、必要に応じてその日の事情に応じた値へ補正できるものとする。
3. VRP PoC が、住所情報の前処理を行ったうえで最適化を実行し、配送ルート案、必要台数、概算コストなどを出力する。住所はそのままでは最適化に利用できないため、PoC 側で住所正規化、ジオコーディング、道路ネットワークへの割当を行い、配送計画に利用可能な地点情報へ変換する。このとき使用台数は完全自由に決めるのではなく、当日のシフトや稼働可能人数を踏まえた台数レンジの中で最適化する。
4. 配送計画担当者が、出力結果を確認し、現場都合に応じて台数選択やルート構成、配送順序の調整を行う。必要に応じて、台数レンジ内での使用台数の選択、各ルートにどの配送先を持たせるかといったルート構成の調整、配送順序の調整を行い、最終計画へ反映する。
5. 確定した配送計画を翌日の配送担当者へ共有し、実配送を行う。ここで PoC の出力は、厳密なナビゲーションそのものではなく、配送順序、移動距離、巡回のまとまりを把握するための業務用計画案として利用する。

例外時は、エラー内容を確認したうえで、必要な修正を行って再実行する。

- 住所不備やジオコーディング失敗の場合は、配送計画担当者が該当住所を修正したうえで再実行する
- 台数レンジや容量制約により実行可能解が得られない場合は、当日の運用条件を見直したうえで再実行する
- 出力ルートが現場感覚と大きく乖離する場合は、代替案の確認または手動調整を行う

このシナリオにおいて、本 PoC が担う役割は、配送先一覧、荷物個数、単一デポに紐づく稼働可能台数レンジをもとに、実道路を前提とした配送計画案を前日に自動生成し、必要台数とルート構成の初期案を提示することである。

また、本 PoC は最終判断を完全自動化するものではなく、計画担当者が経験則や当日の事情を踏まえて最終調整を行う前提の意思決定支援ツールとして位置付ける。

## 3. 解決したい業務課題

本 PoC では、最終的な目的を配送コスト削減に置きつつ、その実現を阻害している業務課題を整理する。ここでは、単に「コストを下げたい」と捉えるのではなく、現場運用と管理判断の両面から、どのような課題が存在し、それに対して本 PoC がどのような価値を返すかを明確にする。

### 3.1 実道路に即した配送計画を作りにくい

対象者：配送計画担当者、実配送担当者、導入判断者  
業務課題：直線距離や簡易距離を前提とした配送計画では、実際の道路事情を十分に反映できず、必要台数や総距離、コスト見積もりの精度が下がりやすい。  
具体例：一方通行や道路接続の影響により、机上では近く見える配送先でも、実運用上は大きく迂回が必要になる。簡易距離でルートを組むと、現場感覚では不自然な巡回順序になりやすい。  
本 PoC による解決の方向性：OSM の実道路ネットワークを用いて距離とルートを算出することで、机上距離ではなく実道路に即した配送計画案を作成し、必要台数や概算コストの精度向上を目指す。

### 3.2 配送計画作成に時間と人手がかかる

対象者：配送計画担当者  
業務課題：翌日の配送先件数が多い場合、担当者が人手で配送順序や台数を検討する必要があり、計画作成に時間がかかる。  
具体例：配送先が多い日に、複数ルートの組み方や必要台数の見積もりに時間を要する。台数を変えた場合の比較案を確認したいときに、同様の検討を繰り返す必要がある。  
本 PoC による解決の方向性：配送先住所と荷物個数から、実道路ベースの初期案を自動生成することで、計画作成にかかる初動工数を下げ、担当者が確認と調整に集中できる状態を目指す。

### 3.3 配送計画が属人化しやすく再現性が低い

対象者：配送計画担当者、導入判断者  
業務課題：配送計画が特定担当者の経験や土地勘に依存すると、担当者が変わったときに計画品質がぶれやすく、再現性が低くなる。  
具体例：同じ配送条件でも、担当者ごとに配送順序や必要台数の判断が変わる。計画根拠が暗黙知化しており、引き継ぎやレビューがしにくい。  
本 PoC による解決の方向性：実道路データと明示的な制約条件に基づいて配送計画案を生成することで、計画作成の土台を標準化し、担当者依存を軽減する。

### 3.4 計画根拠やコスト妥当性を説明しにくい

対象者：配送計画担当者、導入判断者  
業務課題：なぜその台数なのか、なぜそのルートなのか、なぜそのコスト見積もりなのかを定量的に説明しづらいと、社内での納得形成や導入判断が難しくなる。  
具体例：必要台数の判断が経験則中心で、固定費や距離との関係を説明しにくい。ルート案は提示できても、なぜその巡回順序になったかを管理者や現場へ説明しづらい。  
本 PoC による解決の方向性：使用台数、総走行距離、概算コスト、ルート可視化をセットで出力することで、配送計画の根拠を定量的かつ視覚的に説明できる状態を目指す。

### 3.5 複数案の比較や改善余地を把握しにくい

対象者：配送計画担当者、導入判断者  
業務課題：現状のやり方では、単一の配送案は作成できても、他の案と比較してどの程度良いのか、改善余地がどこにあるのかを把握しにくい。  
具体例：台数を 1 台増減した場合に、総距離やコストがどの程度変化するかをすぐに比較できない。現状運用に対して、どれだけ台数削減や距離短縮の余地があるのかが見えにくい。  
本 PoC による解決の方向性：単一の最適解だけでなく、近い代替案や比較可能な指標を出力することで、配送計画担当者と導入判断者が改善余地を具体的に把握できる状態を目指す。

## 4. PoC が提供する業務価値

本 PoC では、前章で整理した業務課題に対して、実道路ネットワークを前提とした配送計画案を返すことで、現場運用と管理判断の両方に価値を提供する。ここでは、各業務課題に対して本 PoC がどのような価値を返すかを明確にする。

### 4.1 実道路に即した配送計画案を得られる価値

対象者：配送計画担当者、実配送担当者、導入判断者  
提供価値：実道路ネットワークに基づく配送ルート案と距離情報を返すことで、机上距離ではなく現実の道路事情に近い配送計画案を作成できる。  
具体的に得られるもの：実道路ベースの配送順序、各区間距離および各ルート総距離、ルート可視化による巡回イメージ。  
この価値が有効である理由：直線距離や簡易距離では把握しづらい一方通行や道路接続の影響を反映できるため、必要台数やルート妥当性の判断精度を高めやすい。

### 4.2 配送計画作成の初動工数を下げられる価値

対象者：配送計画担当者  
提供価値：配送先住所、荷物個数、台数条件を入力として、配送計画の初期案を自動生成することで、担当者が人手でゼロから計画を組む負担を減らせる。  
具体的に得られるもの：必要台数の初期案、ルートごとの配送順序、台数条件を踏まえたルート構成案。  
この価値が有効である理由：担当者は初期案の作成そのものではなく、現場都合を踏まえた確認と調整に時間を使えるようになるため、前日計画業務の効率化につながる。

### 4.3 配送計画の標準化と再現性を高められる価値

対象者：配送計画担当者、導入判断者  
提供価値：経験や土地勘だけに依存せず、同じ入力条件に対して一定のルールで配送計画案を生成することで、計画作成の標準化と再現性向上に寄与する。  
具体的に得られるもの：明示的な入力条件に基づく計画案、担当者が変わっても再利用できる計画作成の土台、調整前後を比較しやすい初期案。  
この価値が有効である理由：計画品質が特定担当者に依存しにくくなり、引き継ぎやレビューがしやすくなるため、業務運用の安定化に寄与する。

### 4.4 計画根拠とコスト妥当性を説明できる価値

対象者：配送計画担当者、導入判断者  
提供価値：使用台数、総走行距離、概算コスト、ルート可視化をセットで返すことで、配送計画の根拠を定量的かつ視覚的に説明できる。  
具体的に得られるもの：台数と距離に基づく概算コスト、ルート案ごとの比較可能な指標、配送順序と巡回構造を示す可視化結果。  
この価値が有効である理由：「なぜその台数なのか」「なぜその順序なのか」を説明しやすくなり、社内の納得形成や導入可否判断を進めやすくなる。

### 4.5 複数案を比較し、改善余地を把握できる価値

対象者：配送計画担当者、導入判断者  
提供価値：単一の最適解だけでなく、近い代替案や比較可能な指標を返すことで、配送計画の改善余地を把握しやすくする。  
具体的に得られるもの：台数やルート構成が異なる複数案、各案の総距離、概算コスト、必要台数、現場調整の判断材料。  
この価値が有効である理由：最適化結果をそのまま採用するだけでなく、運用都合や現場判断を踏まえた比較検討が可能になるため、実務導入しやすい計画支援ツールとして利用しやすくなる。

## 5. 提供する VRP モデルの業務要件

本章では、前章までに整理した業務課題と提供価値を、実際にどのような VRP モデルとして業務へ適用するかという観点で整理する。ここで扱うのは数式レベルの詳細設計ではなく、業務上必要な配送計画の条件を、最適化モデルとしてどう表現するかの前提である。

### 5.1 前提配送計画

本 PoC では、前日に翌日分の配送計画を作成する運用を前提とする。  
入力となる配送対象データは前日時点で確定しており、その情報をもとに翌日の配送計画案を作成する。

配送計画は単一デポ単位で扱い、各車両はデポを起点として出発し、配送完了後は同じデポへ帰着する。  
また、当日配送対象となった荷物は、その日のうちに全量配送することを前提とする。

### 5.2 業務制約条件

本 PoC では、業務上守るべき条件として以下を前提とする。

- 当日配送対象となった配送先は全件訪問する
- 各配送先に割り当てられた荷物数は全量配送する
- 各車両の積載量は車両容量の上限を超えない
- 使用台数は、その日に稼働可能な台数レンジの範囲内とする
- 各車両はデポに帰着した時点で業務終了とする
- 各車両は 1 日 8 時間以内にデポへ戻ることを前提とする
- 各荷物が持つ `午前 / 午後 / 時間指定なし` の時間帯区分に応じて、その時間帯に配送できるようにする

### 5.3 最適化対象

本 PoC では、業務上の主目的をコスト削減に置く。  
コストは簡略化して、`使用台数 × 1台あたり固定コスト + 総走行距離 × 距離比例コスト` として表現する。

このうち、業務上は 1 台あたり固定コストの影響が大きいと想定するため、結果として使用台数を抑えることを強く重視する。  
そのうえで、同じ台数条件の中では総走行距離を抑え、距離比例コストを下げることを第二の最適化対象とする。

### 5.4 モデル前提

本 PoC で扱う VRP モデルは、業務要件を簡略化して以下の前提で表現する。

- 需要量は荷物個数で表現する
- 車両容量も荷物個数で表現する
- 距離は OSM の実道路ネットワークを用いて算出する
- 使用台数は固定値ではなく、入力された台数レンジの中で決定する
- 配送先住所は、そのままでは最適化に利用せず、PoC 側で住所正規化、ジオコーディング、道路ネットワークへの割当を行ったうえで利用する
- 入力として与えられる配送情報は、住所情報、荷物個数、配送日を基本とする
- 台数コストは単一デポに紐づくパラメータとして扱う

### 5.5 入出力の位置づけ

本 PoC における入力と出力の位置づけは以下のとおりとする。

入力として必要なもの

- 配送先情報
- 各配送先に紐づく荷物個数
- 配送日
- 単一デポ情報
- 稼働可能台数レンジ
- デポに紐づく台数コスト
- 距離比例コスト

出力として返すもの

- 必要台数
- 配送順序を含むルート案
- 概算コスト
- 地図上で確認できるルート可視化
- 台数レンジ内で比較可能な代替案
- 各ルートの総距離
- 各区間距離
- `午前 / 午後 / 時間指定なし` のどの時間帯で配送する想定かを示す到着時間帯情報

ルート案の出力では、配送担当者が巡回順序を理解できるよう、各配送先に訪問順を付番した状態で確認できることを前提とする。  
また、一覧形式の出力では、`配送先1`、`配送先2` といった形で訪問順序を確認できる構成を想定する。

代替案は、台数レンジの中で異なる台数案やルート構成案を比較できる形で出力し、配送計画担当者が当日の運用条件や現場都合に応じて採用案を判断できるようにする。

## 6. 入力データ / 出力データ要件

本章では、VRP PoC を実行するために必要な入力データと、PoC の結果として返す出力データを整理する。ここではまず入力データを、マスタデータ、トランザクションデータ、前処理で付与される項目に分けて定義する。

### 6.1 マスタデータ

| 項目名 | データ種別 | 説明 | データ型 | 必須 | 生成タイミング | 備考 |
|---|---|---|---|---|---|---|
| depot_id | デポ識別子 | 単一デポを識別するための ID | string | 必須 | 事前保持 | デポ単位で一意な値 |
| depot_name | 文字列 | デポ名称 | string | 必須 | 事前保持 | 表示用名称 |
| depot_address | 住所 | デポ所在地の住所情報 | string | 必須 | 事前保持 | 元データとして保持 |
| depot_latitude | 座標 | デポの緯度 | float | 任意 | 前処理後付与 | 住所からジオコーディングにより付与 |
| depot_longitude | 座標 | デポの経度 | float | 任意 | 前処理後付与 | 住所からジオコーディングにより付与 |
| vehicle_count_min | 数値 | 稼働可能台数の下限値 | integer | 必須 | 事前保持 | 必要に応じて実行時補正可 |
| vehicle_count_max | 数値 | 稼働可能台数の上限値 | integer | 必須 | 事前保持 | 必要に応じて実行時補正可 |
| fixed_cost_per_vehicle | 数値 | 1 台あたり固定コスト | float | 必須 | 事前保持 | デポ単位パラメータ |
| capacity_per_vehicle | 数値 | 1 台あたり積載上限個数 | integer | 必須 | 事前保持 | 荷物個数ベース |
| work_start_time | 時刻 | 業務開始時刻 | time | 必須 | 事前保持 | デポ出発基準時刻 |
| work_end_time | 時刻 | 業務終了時刻 | time | 必須 | 事前保持 | 1 日 8 時間以内帰着の基準 |
| distance_unit_cost | 数値 | 距離比例コスト | float | 必須 | 事前保持 | デポ単位または共通パラメータとして扱う |

本 PoC では `人数 = 台数` を前提とするため、稼働可能台数レンジはそのまま当日に確保可能な人員レンジとしても扱う。

### 6.2 トランザクションデータ

| 項目名 | データ種別 | 説明 | データ型 | 必須 | 生成タイミング | 備考 |
|---|---|---|---|---|---|---|
| delivery_id | 配送識別子 | 各配送先レコードを識別する ID | string | 必須 | 入力時 | 配送単位で一意な値 |
| delivery_date | 日付 | 対象となる配送日 | date | 必須 | 入力時 | 日次計画の基準日 |
| depot_id | デポ識別子 | 当該配送データが紐づくデポ ID | string | 必須 | 入力時 | 単一デポ前提でも明示保持する |
| destination_name | 文字列 | 配送先名称 | string | 任意 | 入力時 | 表示用名称 |
| destination_address | 住所 | 配送先所在地の住所情報 | string | 必須 | 入力時 | 元データとして保持 |
| destination_latitude | 座標 | 配送先の緯度 | float | 任意 | 前処理後付与 | 住所からジオコーディングにより付与 |
| destination_longitude | 座標 | 配送先の経度 | float | 任意 | 前処理後付与 | 住所からジオコーディングにより付与 |
| package_count | 数値 | 配送先に届ける荷物個数 | integer | 必須 | 入力時 | 本 PoC では需要量として扱う |
| delivery_time_slot | 区分値 | 到着予定区分 | string | 必須 | 入力時 | `午前 = 09:00-13:00 / 午後 = 13:00-18:00 / 時間指定なし` を想定 |

### 6.3 前処理で付与される項目

| 項目名 | データ種別 | 説明 | データ型 | 必須 | 生成タイミング | 備考 |
|---|---|---|---|---|---|---|
| normalized_address | 住所 | 表記揺れ補正後の住所情報 | string | 任意 | 前処理 | 元住所を補正した値 |
| geocode_status | 区分値 | ジオコーディング成否 | string | 任意 | 前処理 | 例: success / failed |
| network_node_id | 識別子 | 道路ネットワーク上で割り当てたノード ID | string | 任意 | 前処理 | 最適化計算用の参照情報 |
| snap_status | 区分値 | 道路ネットワーク割当成否 | string | 任意 | 前処理 | 例: success / failed |
| estimated_travel_time | 数値 | 静的推定による所要時間 | float | 任意 | 前処理 | 補正前提の参考値または制約用 |

OSM のノード・エッジ属性そのものは入力データに含めず、ネットワーク基盤として別管理する。入力データ側には、住所正規化やジオコーディング、道路ネットワークへの割当により得られた参照情報のみを付与し、実行時に内部で結合して利用する。

### 6.4 出力データ

本 PoC では、業務利用者が判断に使えるよう、表形式で確認できる出力データを返す。出力データは、配送計画全体を確認するためのサマリ、各ルートの内容を確認するための明細、台数レンジ内での比較を行うための代替案比較に分けて扱う。

#### 6.4.1 配送計画サマリ

| 項目名 | データ種別 | 説明 | データ型 | 必須 | 生成タイミング | 備考 |
|---|---|---|---|---|---|---|
| plan_id | 識別子 | 計画結果を識別する ID | string | 必須 | 出力時 | 1 回の PoC 実行単位 |
| delivery_date | 日付 | 対象となる配送日 | date | 必須 | 出力時 | 日次計画単位 |
| depot_id | デポ識別子 | 対象デポ ID | string | 必須 | 出力時 | 単一デポ前提 |
| selected_vehicle_count | 数値 | 採用した使用台数 | integer | 必須 | 出力時 | 計画全体の台数 |
| total_distance | 数値 | 計画全体の総走行距離 | float | 必須 | 出力時 | 単位は km 想定 |
| total_cost | 数値 | 計画全体の概算コスト | float | 必須 | 出力時 | 固定費 + 距離比例費 |
| total_stop_count | 数値 | 訪問配送先数 | integer | 必須 | 出力時 | 全件訪問確認用 |
| total_package_count | 数値 | 配送荷物総数 | integer | 必須 | 出力時 | 全量配送確認用 |
| return_within_work_hours | 真偽値 | 8 時間以内帰着を満たすか | boolean | 必須 | 出力時 | 時間制約確認用 |

#### 6.4.2 ルート明細

| 項目名 | データ種別 | 説明 | データ型 | 必須 | 生成タイミング | 備考 |
|---|---|---|---|---|---|---|
| plan_id | 識別子 | 対象計画 ID | string | 必須 | 出力時 | 計画単位で紐づく |
| route_id | 識別子 | ルート識別子 | string | 必須 | 出力時 | 車両単位のルート |
| vehicle_no | 数値 | 車両番号 | integer | 必須 | 出力時 | 表示用番号 |
| stop_order | 数値 | 訪問順序 | integer | 必須 | 出力時 | 1, 2, 3... の順序 |
| delivery_id | 配送識別子 | 対象配送 ID | string | 必須 | 出力時 | 配送先単位で紐づく |
| destination_name | 文字列 | 配送先名称 | string | 任意 | 出力時 | 表示用名称 |
| destination_address | 住所 | 配送先住所 | string | 必須 | 出力時 | 確認用 |
| package_count | 数値 | 当該配送先の荷物個数 | integer | 必須 | 出力時 | 需要量 |
| requested_time_slot | 区分値 | 指定された到着予定区分 | string | 必須 | 出力時 | `午前 / 午後 / 時間指定なし` |
| planned_time_slot | 区分値 | 計画上の到着想定時間帯 | string | 必須 | 出力時 | `午前 = 09:00-13:00 / 午後 = 13:00-18:00 / 時間指定なし` |
| segment_distance | 数値 | 直前地点から当該配送先までの距離 | float | 必須 | 出力時 | 各区間距離 |
| route_total_distance | 数値 | 当該ルートの総走行距離 | float | 必須 | 出力時 | ルート単位距離 |
| cumulative_distance | 数値 | ルート開始からの累積距離 | float | 任意 | 出力時 | 進行状況確認用 |

#### 6.4.3 代替案比較

| 項目名 | データ種別 | 説明 | データ型 | 必須 | 生成タイミング | 備考 |
|---|---|---|---|---|---|---|
| candidate_plan_id | 識別子 | 代替案識別子 | string | 必須 | 出力時 | 案ごとに一意 |
| delivery_date | 日付 | 対象配送日 | date | 必須 | 出力時 | 日次計画単位 |
| depot_id | デポ識別子 | 対象デポ ID | string | 必須 | 出力時 | 単一デポ前提 |
| vehicle_count | 数値 | 当該案の使用台数 | integer | 必須 | 出力時 | 台数が異なれば別案として扱う |
| total_distance | 数値 | 当該案の総走行距離 | float | 必須 | 出力時 | 距離比較用 |
| total_cost | 数値 | 当該案の概算コスト | float | 必須 | 出力時 | コスト比較用 |
| rank | 数値 | 評価順位 | integer | 必須 | 出力時 | 上位案から順に付与 |
| selected_flag | 真偽値 | 採用案かどうか | boolean | 必須 | 出力時 | 最終採用判断用 |

### 6.5 可視化成果物

本 PoC では、表形式データに加えて、配送計画担当者および実配送担当者が視覚的に確認できる成果物を出力する。

- 全体ルート図  
  計画全体のルート構成を俯瞰できる可視化。デポ位置、配送先位置、ルートごとの色分けを含む。

- ルート別可視化  
  各車両ルートごとの巡回経路を確認できる可視化。ルート単位で拡大し、現場が確認しやすい粒度で表示する。

- 訪問順序付き可視化  
  配送先ごとに `1, 2, 3...` の訪問順を付番して表示する可視化。配送担当者が巡回順序を直感的に理解できるようにする。

- 代替案比較可視化  
  台数レンジ内で生成した複数案を比較するための可視化。台数、総距離、概算コストの違いを確認しやすい形で出力する。

## 7. PoC 成立に必要な基盤要件

### 7.1 道路ネットワーク基盤

本 PoC では、道路ネットワークの正本として OpenStreetMap を利用する。対象は、デポが存在する都道府県および配送対象エリアを含む道路ネットワークとし、取得したデータはローカルに保存して再利用可能な状態で保持する。

道路ネットワーク構築時には、毎回 OSM へ再取得を行うのではなく、ローカル保存済みの道路グラフを参照する。これにより、データ取得にかかる時間を削減するとともに、同一データを用いた再実行を可能にし、PoC の再現性を担保する。

また、道路ネットワークデータは一度取得して終わりではなく、一定周期で更新可能な運用を前提とする。更新頻度については、OSM 側の更新特性を確認したうえで、年 1 回または半年に 1 回程度を候補として見直す。

### 7.2 住所前処理基盤

本 PoC では、住所前処理基盤として別リポジトリの `geo_enrichment_streamlit` を利用する。配送対象データとして受け取る住所情報は、そのままでは最適化に利用できないため、住所正規化とジオコーディングを行い、緯度経度を付与したうえで後続処理へ渡す。

住所前処理基盤を独立して持つ理由は、実務データでは都道府県名、市区町村名、番地表記などに表記揺れや欠損が含まれる可能性があり、OSM の道路ネットワーク処理だけでは安定的に扱いにくいためである。そのため、本 PoC では道路ネットワーク取得・距離計算とは別に、住所を整形し、位置情報へ変換するための専用基盤を設ける。

この基盤により、VRP PoC 側では、住所をそのまま扱うのではなく、前処理済みの住所情報と緯度経度を受け取ることで、配送地点の道路ネットワーク割当や距離計算を安定して実行できる状態を目指す。

また、住所前処理の結果として、住所正規化後の値、ジオコーディング成否、付与された緯度経度を保持し、住所不備がある場合には配送計画担当者が該当住所を修正したうえで再実行できる運用を前提とする。

### 7.3 配送地点割当基盤

本 PoC では、住所前処理によって得られたデポおよび配送先の緯度経度を、OSM 道路ネットワーク上の計算可能な地点へ割り当てる基盤を持つ。ここでいう割当とは、各配送地点を道路ネットワーク上のノードまたは計算上の代表地点へ対応づけ、距離計算および VRP の入力として利用できる状態にすることである。

配送地点は、単に緯度経度を持つだけでは道路ネットワーク上の道のり距離計算に直接利用できないため、最寄りの道路ネットワーク地点への対応づけが必要になる。そのため、本 PoC では、デポおよび配送先ごとに道路ネットワーク上の参照地点を付与し、その割当結果を後続の距離計算および最適化処理で利用する。

また、割当結果は毎回異なるものではなく、同一入力に対して安定的に再現できることを前提とする。割当が失敗した場合や不自然な地点へ対応づいた場合には、住所前処理結果や入力住所を見直したうえで再実行できる運用とする。

### 7.4 距離・時間計算基盤

本 PoC では、道路ネットワーク上に割り当てたデポおよび配送先の参照地点をもとに、配送計画に必要な距離および所要時間を計算する基盤を持つ。ここで算出した距離は、ルートの評価、コスト算出、代替案比較の基礎として利用する。

距離については、OSM 道路ネットワーク上の道のり距離を用いて計算する。これにより、直線距離ではなく、実際の道路接続や一方通行を踏まえた距離をルート計画へ反映する。

また、本 PoC では `午前 / 午後 / 時間指定なし` および `1 日 8 時間以内帰着` の時間制約を扱うため、所要時間の計算も必要となる。所要時間は、OSM 由来の道路属性と速度仮定に基づく静的な推定値を用いる。

ただし、この所要時間は実交通や渋滞を反映した動的時間ではないため、そのまま現実の到着時刻を保証するものではない。そのため、本 PoC では静的所要時間を時間制約判定の基準として利用しつつ、必要に応じて一律係数や道路種別別係数などの補正を適用する想定で扱う。

### 7.5 出力生成基盤

本 PoC では、前処理、配送地点割当、距離・時間計算、最適化処理によって得られた結果を、業務利用者が確認・判断できる形式へ加工して出力する基盤を持つ。

表形式の出力では、配送計画全体を確認するためのサマリ、各ルートの内容を確認するための明細、台数レンジ内での比較を行うための代替案比較を生成する。これにより、配送計画担当者は必要台数、総距離、概算コスト、配送順序、各区間距離を確認しながら、採用案を判断できる状態を目指す。

また、可視化出力では、ルート全体図、ルート別図、訪問順序付き可視化、代替案比較可視化を生成する。台数が変わることによってルート構成や巡回順序がどう変化したかについては、主に可視化成果物側で確認できる形とする。

出力生成基盤では、単にデータを保存するだけでなく、業務上の確認と説明に使える粒度と形式で安定して出力できることを重視する。

### 7.6 再現性・運用基盤

本 PoC では、同じ入力データ、同じ道路ネットワークデータ、同じ前処理条件、同じ最適化設定を用いた場合に、同じ結果を再現できる状態を目指す。ここでいう再現性とは、毎回まったく同じ処理条件で実行した際に、同一の配送計画案、比較結果、可視化成果物を再生成できることである。

そのため、本 PoC では以下の運用を前提とする。

- OSM から取得した道路ネットワークはローカルに保存し、毎回の再取得ではなく保存済みデータを参照する
- 住所前処理結果や道路ネットワーク割当結果を保持し、同じ入力に対して同じ前処理結果を再利用できるようにする
- 使用した入力データ、コストパラメータ、台数レンジ、時間帯制約、補正条件などの実行条件を記録する
- 出力された計画結果、代替案比較、可視化成果物を実行単位で対応づけて保存する

一方で、OSM データ更新、コスト条件変更、稼働可能台数レンジ変更などがあった場合には、結果が変化しうることを前提とする。そのため、再現性は「常に同じ答えを返すこと」ではなく、「同じ条件であれば同じ結果を再生成できること」として扱う。

また、運用上は道路ネットワークデータの更新時期、前処理結果の再生成タイミング、入力条件変更時の再実行ルールを定めることで、PoC を継続的に検証可能な状態で維持する。

## 8. 業務上の成功基準

本 PoC では、単に最適化結果を出力できることではなく、業務上の意思決定や現場運用に利用できる状態になっていることを成功とみなす。そのため、成功基準は対象者ごとに期待される効果とあわせて整理する。

| 対象者 | 効果分類 | 成功基準 | 成功とみなす状態 |
|---|---|---|---|
| 配送計画担当者 | ルート最適化 | 実道路に基づく配送計画を作成できる | 直線距離ではなく、実道路距離と道路接続を踏まえたルート案を作成できる |
| 配送計画担当者 | コスト判断 | 必要台数、総距離、概算コストを比較判断できる | 台数レンジ内で複数案を比較し、どの案を採用するか判断できる |
| 配送計画担当者 | 工数削減 | 計画作成の初動工数を削減できる | 人手でゼロから配車案を組むのではなく、PoC の初期案をもとに確認と調整へ集中できる |
| 実配送担当者 | 現場受容性 | 出力されたルート内容を理解できる | 訪問順序、巡回ルート、時間帯の想定を見て、翌日の配送イメージを把握できる |
| 実配送担当者 | 実行可能性 | 実際に走行可能な配送計画である | ルートが道路接続上成立しており、現場感覚と大きく乖離しない順序で構成されている |
| 導入判断者 | 説明可能性 | 計画根拠を説明できる | 必要台数、距離、概算コスト、ルート可視化を用いて、なぜその計画なのかを説明できる |
| 導入判断者 | 投資判断 | 改善余地を把握できる | 現状運用と比較して、台数削減や距離短縮、コスト改善の余地を確認できる |
| 導入判断者 | PoC 妥当性 | 次段階へ進めるだけの精度と再現性がある | 同じ条件で同じ結果を再生成でき、業務判断に使える粒度の出力が得られる |

## 9. PoC の可否判断基準

本章では、本プロジェクトを社内 PoC レベルの検証として見たときに、次段階へ進めてよいかを判断するための基準を定義する。ここでいう次段階とは、詳細設計の継続、実装の具体化、評価実験の実施へ進むことを指す。

以下の条件を満たす場合、本 PoC は次段階へ進めてよいものと判断する。

- [ ] OSM を用いた道路ネットワーク基盤を安定して取得・保存・再利用できること  
  完了日時：  
  同じ対象エリアに対して、毎回の再取得に依存せず、保存済みデータを用いて安定して処理を再実行できる状態であること。

- [ ] 実道路ネットワーク上で配送計画を安定して実行できること  
  完了日時：  
  デポおよび配送先を道路ネットワークへ適切に割り当て、OD 間距離やルート計算が破綻せずに成立すること。

- [ ] 業務制約を加味したうえで実行可能な解を生成できること  
  完了日時：  
  単一デポ、全件訪問、荷物個数ベースの容量制約、稼働可能台数レンジ、`午前 / 午後 / 時間指定なし` の時間帯制約、1 日 8 時間以内帰着といった条件を満たす配送計画を扱えること。

- [ ] 台数レンジ内で比較可能な複数案を生成できること  
  完了日時：  
  単一の最適解だけでなく、使用台数やルート構成が異なる複数案を比較可能な形で出力し、業務判断に利用できること。

- [ ] 出力結果が現場で確認しやすい形に整理されていること  
  完了日時：  
  必要台数、配送順序、各区間距離、各ルート総距離、概算コスト、時間帯情報、可視化成果物が、配送計画担当者および実配送担当者にとって理解しやすい形で出力されること。

- [ ] 同じ条件で同じ結果を再生成できること  
  完了日時：  
  同一の入力データ、道路ネットワークデータ、前処理条件、最適化条件を用いた場合に、同一の結果を再現できること。

- [ ] 業務負荷を維持または削減しながら、コスト削減余地を示せること  
  完了日時：  
  新たな運用負荷を過度に増やさず、現状の配送計画と比較して、必要台数、総走行距離、概算コストのいずれかに改善余地を示せること。
