# 再現用ピン固定バージョン（動作確認済み環境: Python 3.12.1）
# 通常インストール: pip install -e .[dev]
# 固定版インストール: pip install -r requirements.txt && pip install -e .

# --- core ---
osmnx==2.1.0
networkx==3.2.1
ortools==9.14.6206
pandas==2.1.4
numpy==1.26.4
geopandas==1.1.3
shapely==2.1.2
folium==0.20.0
openpyxl==3.1.5
PyYAML==6.0.1
click==8.1.7
requests==2.32.2

# --- dev ---
pytest==7.4.4
pytest-cov>=4.0
types-requests>=2.31
